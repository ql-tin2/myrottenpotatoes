source 'https://rubygems.org'

gem 'rails', '3.2.2'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

gem 'sqlite3'

#by SAAS course - Berkeley
gem 'haml'
gem 'ruby-tmdb'
# use Ruby debugger
group	:development, :test do
	gem 'rspec-rails' #for chapter 05 on rspec
	gem 'ZenTest'     #for chapter 05 for autotest
	gem 'cucumber-rails'
	gem 'cucumber-rails-training-wheels' #some prefabbed step definitions
	gem 'database_cleaner' # to clean Cucumber's test database between runs
	gem 'capybara'
	gem 'launchy' # a useful debugging aid for user stories
	gem 'ruby-debug19'
end
#


# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails',   '~> 3.2.3'
  gem 'coffee-rails', '~> 3.2.1'

  # See https://github.com/sstephenson/execjs#readme for more supported runtimes
  #SAAS course use 'therubyracer'
  gem 'therubyracer'

  gem 'uglifier', '>= 1.0.3'
end

gem 'jquery-rails'

# To use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'ruby-debug19', :require => 'ruby-debug'
